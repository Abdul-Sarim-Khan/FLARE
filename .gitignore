# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
*.egg
dist/
build/
.eggs/

# Virtual environments
venv/
.venv/
env/
.env/

# FLARE runtime artifacts (machine-specific, generated by setup)
client/agent.env
server/data/server.env
server/data/flare.db
server/data/flare.db-shm
server/data/flare.db-wal

# PKI artifacts (generated per-install — never commit private keys or client bundles)
server/certs/

# Client cert files (machine-specific, never commit to repo)
client/certs/

# pktmon output (generated at runtime)
client/net_flows.csv

# Model artifacts — ignore generated/training pkl files but keep deployed models
*.pkl
!client/network/models/network_mlp.pkl
!client/network/models/network_scaler.pkl

# IDE / OS
.vscode/
.idea/
*.suo
*.user
Thumbs.db
desktop.ini
.DS_Store
